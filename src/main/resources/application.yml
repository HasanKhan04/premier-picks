spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/stats
    username: postgres
    password: hasan
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show_sql: true
